{
  "name": "fl-journeys",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "parcel": "^2.4.1",
    "push-dir": "^0.4.1"
  },
  "scripts": {
    "start": "yarn parcel src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "builds": "parcel build src/index.html",
    "push-gh-pages": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "author": "",
  "license": "ISC"
}
